# 银龄乐园 微信小程序 PRD

## 1. 产品概述

**产品名称:** 银龄乐园  
**平台:** 微信小程序  
**目标用户:** 老年用户（主要）、年轻子代用户（次要）  
**核心价值:** 让老年人能够轻松使用的社交与信息平台，同时支持老年人记录和传承人生故事。

---

## 2. 用户角色

| 角色 | 描述 | 主要行为 |
|------|------|----------|
| **老人用户** | 60 岁以上用户，适老化设计优先 | 发布动态、回答问题、阅读资讯、管理回忆录 |
| **年轻用户** | 子代或年轻人 | 提问、点赞互动、浏览长辈动态 |

---

## 3. 全局设计要求（适老化）

- 所有正文字体不低于 18px，标题不低于 22px
- 可点击元素最小高度 60px，内边距充足，避免误触
- 主色调暖色系（橙色系），高对比度，避免小号灰色文字
- 核心操作步骤控制在 3 步以内
- 所有操作成功/失败给出明显的视觉反馈（toast 提示）
- 语音输入入口在发布场景中优先显眼展示

---

## 4. 功能模块

### 4.1 身份选择与进入

用户首次打开小程序时，选择自己的身份（老人 / 年轻人）并输入昵称，完成后进入首页。用户信息本地缓存，再次打开自动跳过该步骤。

**页面元素**
- 两个大卡片按钮的身份选项（老人 / 年轻人）
- 昵称输入框
- 进入按钮

**验收标准**
- 完成选择 + 输入昵称后，1 次点击进入首页
- 再次打开小程序自动进入首页，无需重新选择

---

### 4.2 首页

根据用户身份展示个性化欢迎文案，并提供各功能模块的快捷入口卡片。

**老人用户入口**
- 资讯科普
- 社区问答
- 我的主页（含动态与回忆录）

**年轻用户入口**
- 资讯科普
- 社区问答
- 长辈动态

**验收标准**
- 两个身份的首页内容差异正确体现
- 所有入口卡片点击后跳转正确

---

### 4.3 老人主页

老人的个人中心，包含动态发布、自己的动态列表，以及人生回忆录入口。

#### 4.3.1 发布动态
- 支持文本输入发布
- 支持语音输入（微信内置语音识别，转文字后填入输入框，可二次编辑）
- 发布前内容安全检测，拦截诈骗关键词与不文明用语，不通过时给出友好提示
- 发布成功给出明显成功提示

#### 4.3.2 我的动态列表
- 展示当前用户的所有动态：内容、发布时间、点赞数
- 可对他人动态点赞
- 每条动态提供「加入回忆录」操作入口

**验收标准**
- 语音转文字结果可在提交前编辑
- 诈骗关键词与敏感词审核生效
- 点赞数实时更新
- 「加入回忆录」操作成功后有反馈提示

---

### 4.4 长辈动态（公共 Feed）

展示所有老人用户发布的动态，供年轻用户浏览和互动。

- 展示所有老人发布的动态（时间倒序）
- 可对动态点赞
- 年轻用户在此页无发布入口

**验收标准**
- 仅老人发布的内容出现在此页
- 年轻用户点赞正常生效

---

### 4.5 社区问答

年轻用户提问，老人用户回答，传递经验与智慧。

#### 4.5.1 提问（年轻用户）
- 填写问题标题（必填）和问题详情
- 发布前内容安全检测

#### 4.5.2 回答（老人用户）
- 在问题详情页展开回答输入框
- 支持文本输入与语音输入
- 发布前内容安全检测

#### 4.5.3 问答列表与详情
- 列表展示所有问题：提问者、时间、回答数
- 详情页展示问题全文与所有回答
- 可对回答点赞（「感谢」按钮）

**验收标准**
- 提问入口仅年轻用户可见，回答入口仅老人用户可见
- 点赞数正确更新
- 内容审核拦截不当内容

---

### 4.6 资讯科普

为老年用户提供精选资讯，支持语音朗读。

#### 4.6.1 内容来源

资讯内容从**人民日报官网**（http://www.people.com.cn）抓取，通过后端定时任务（每日更新一次）爬取最新文章并存入数据库。

抓取策略：
- 抓取要闻、健康、社会等频道的最新文章
- 每篇文章保存：标题、正文、摘要（取正文前 100 字）、来源 URL、发布时间、分类
- 封面图可选保存
- 若当日抓取失败，保留上次缓存内容，不展示空白页

#### 4.6.2 资讯列表
- 展示资讯卡片：分类标签、标题、摘要、日期
- 支持按分类筛选（全部 / 要闻 / 健康养生 / 社会生活 / 时政 等，分类与抓取频道对应）
- 列表按发布时间倒序排列
- 点击卡片进入详情页

#### 4.6.3 资讯详情
- 大字体展示全文
- 显示来源（人民日报）与发布时间
- 支持语音朗读：开始 / 暂停 / 停止
- 语速选择：慢速 / 正常 / 快速，默认慢速
- 页面顶部显示防诈骗温馨提示

**验收标准**
- 朗读控制（开始 / 暂停 / 停止）正常工作
- 语速切换立即生效
- 分类筛选结果准确
- 每日刷新后可见当天最新内容

---

### 4.7 人生回忆录（新功能）

老人专属功能。帮助老年用户将生活动态整理成人生回忆录，支持 AI 辅助撰写。

**入口位置:** 老人主页显著区域，独立「我的回忆录」按钮。

#### 4.7.1 回忆录主页
- 展示已创建的回忆录片段列表（标题 / 摘要 / 时间）
- 点击查看全文
- 「新建」按钮

#### 4.7.2 从动态/回答中选取素材
- 列出用户所有已发布的动态与问答回答，支持多选
- 点击「下一步」进入 AI 辅助写作，或直接手动创建

#### 4.7.3 AI 辅助写作
- 基于所选素材，调用 AI 生成一段连贯的回忆录段落草稿（约 150-300 字）
- 生成期间显示 loading 状态
- 生成内容可在页面内直接编辑修改
- 编辑完成后保存为一篇回忆录

#### 4.7.4 手动创建
- 不选素材，直接手动输入标题与正文，保存即可

**数据**
- 每篇回忆录包含：标题（可选）、正文内容、关联来源 ID 列表、创建时间
- 仅作者本人可见

**验收标准**
- 至少选 1 条动态/回答才能触发 AI 生成
- AI 生成期间 loading 状态正确显示
- 生成内容可完整编辑后保存
- 保存成功后，回忆录列表即刻更新

---

## 5. 数据模型

### User
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 微信 openid |
| nickname | string | 昵称 |
| role | enum | elder / young |

### Post（动态）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 动态 ID |
| authorId | string | 发布者 ID |
| content | string | 正文内容 |
| likes | number | 点赞数 |
| createdAt | timestamp | 发布时间 |

### Question（问题）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 问题 ID |
| authorId | string | 提问者 ID |
| title | string | 标题 |
| content | string | 详情 |
| createdAt | timestamp | 发布时间 |

### Answer（回答）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 回答 ID |
| questionId | string | 所属问题 ID |
| authorId | string | 回答者 ID |
| content | string | 回答内容 |
| likes | number | 点赞数 |
| createdAt | timestamp | 发布时间 |

### News（资讯）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 资讯 ID |
| title | string | 标题 |
| summary | string | 摘要 |
| content | string | 全文 |
| category | string | 分类 |
| image | string | 封面图 URL（可选） |
| createdAt | timestamp | 发布时间 |

### Memoir（回忆录）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 回忆录 ID |
| authorId | string | 创建者 ID |
| title | string | 标题（可选） |
| content | string | 正文内容 |
| sourceRefs | string[] | 来源动态/回答 ID 列表 |
| createdAt | timestamp | 创建时间 |

---

## 6. AI 能力需求

| 能力 | 使用场景 | 方案 |
|------|----------|------|
| 语音转文字（ASR） | 动态发布、问答回答的语音输入 | 微信录音 + 第三方 ASR（如讯飞/阿里云） |
| 文字转语音（TTS） | 资讯朗读 | 微信 `wx.createInnerAudioContext` + TTS 接口 |
| 内容安全审核 | 所有用户生成内容发布前检测 | 微信云调用 `msgSecCheck` 接口 |
| 回忆录 AI 生成 | 人生回忆录辅助写作 | 通用 LLM HTTP 接口，模型待定 |

### 6.1 回忆录 LLM 接口配置

接口调用通过后端服务转发（不在小程序前端直接暴露密钥）。后端需预留以下配置项：

```
LLM_API_URL=   # 待填入，例如 https://api.xxx.com/v1/chat/completions
LLM_API_KEY=   # 待填入
```

请求格式兼容 OpenAI Chat Completions 标准：`POST` 请求，Body 包含 `messages` 数组，读取返回值 `choices[0].message.content`。

### 6.2 回忆录生成 Prompt

```
你是一个帮助老人书写人生回忆录的助手。以下是这位老人发布的一些日常生活片段：

{用户选择的动态/回答内容列表，每条以「-」开头换行}

请用温暖、朴实、第一人称的语气，将以上内容整理成一段连贯的回忆录段落，约 150-300 字。不要虚构原文中没有的信息。
```

---

## 7. 页面结构

```
底部 Tab 导航（老人身份）
├── 首页
├── 资讯科普
├── 社区问答
└── 我的主页
    ├── 发布动态
    ├── 我的动态列表
    └── 我的回忆录
        ├── 回忆录列表
        ├── 新建（选素材 → AI 生成 → 编辑 → 保存）
        └── 新建（手动创建）

底部 Tab 导航（年轻用户身份）
├── 首页
├── 资讯科普
├── 社区问答
└── 长辈动态
```

---

## 8. 不做功能（明确排除）

- 直播 / 开播 / 弹幕
- 用户关注 / 私信
- 播客 / 音频收听
- 后台运营管理系统
- 付费功能

---

## 9. 成功指标（MVP）

- 老人用户动态发布成功率 ≥ 80%
- 资讯朗读功能使用率 ≥ 30%
- 社区问答回答率 ≥ 20%
- 回忆录功能使用率 ≥ 10%

---

## 10. 待确认问题

1. 是否需要用户数据删除与隐私管理功能？
2. 回忆录片段是否支持生成图片卡片分享给家人？
3. 人民日报内容爬取需确认是否符合其版权与 robots.txt 要求；如有限制，可改为接入第三方授权新闻 API。

